<% content_for :title, "Settings" %>

<div class="container">
  <div class="page-header">
    <h1>Settings</h1>
  </div>

  <%# Personal information %>
  <p class="settings-section-label">Personal information</p>
  <div class="settings-info-card">

    <%# Profile picture %>
    <div class="settings-avatar-section" data-controller="avatar-preview inline-confirm">
      <p class="settings-avatar-section__heading">Profile picture</p>
      <div class="settings-avatar-row">

        <%# Current avatar (large) %>
        <div class="settings-avatar-current">
          <% if Current.user.avatar.attached? && Current.user.avatar.blob.persisted? %>
            <%= image_tag Current.user.avatar, class: "profile-avatar-img", alt: "Profile photo" %>
          <% else %>
            <div class="user-avatar profile-avatar-placeholder">
              <%= Current.user.display_name.first.upcase %>
            </div>
          <% end %>
        </div>

        <%# Predefined avatar grid + upload button %>
        <div class="settings-avatar-grid">
          <% [
            { bg: "#f87171", emoji: "🐧" },
            { bg: "#a78bfa", emoji: "🦁" },
            { bg: "#34d399", emoji: "🐼" },
            { bg: "#fb923c", emoji: "🐸" },
            { bg: "#60a5fa", emoji: "🦊" },
            { bg: "#f472b6", emoji: "🐨" },
            { bg: "#4ade80", emoji: "🦅" },
            { bg: "#facc15", emoji: "🐯" },
            { bg: "#f43f5e", emoji: "🦋" },
            { bg: "#38bdf8", emoji: "🐬" },
            { bg: "#c084fc", emoji: "🦜" },
            { bg: "#2dd4bf", emoji: "🐻" },
            { bg: "#fb7185", emoji: "🦄" },
            { bg: "#a3e635", emoji: "🐙" },
            { bg: "#fbbf24", emoji: "🦒" },
            { bg: "#818cf8", emoji: "🐳" },
            { bg: "#34d399", emoji: "🦚" },
            { bg: "#f97316", emoji: "🐺" },
            { bg: "#e879f9", emoji: "🦝" },
            { bg: "#6ee7b7", emoji: "🐮" },
            { bg: "#fcd34d", emoji: "🦉" },
          ].each do |av| %>
            <button type="button"
                    class="settings-avatar-option"
                    style="background-color: <%= av[:bg] %>;"
                    aria-label="Select avatar">
              <%= av[:emoji] %>
            </button>
          <% end %>

          <%# Upload button %>
          <%= form_with url: account_path, method: :patch, multipart: true do |av| %>
            <%= av.hidden_field :section, value: "profile" %>
            <label for="avatar-file-input"
                   class="settings-avatar-option settings-avatar-option--add"
                   title="Upload a custom photo">+</label>
            <%= av.file_field :avatar,
                  id: "avatar-file-input",
                  accept: "image/jpeg,image/png,image/webp",
                  class: "profile-avatar-input",
                  data: {
                    "avatar-preview-target": "input",
                    action: "change->avatar-preview#change"
                  } %>
          <% end %>
        </div>

      </div>

      <% if Current.user.errors[:avatar].any? %>
        <p class="profile-avatar-error"><%= Current.user.errors[:avatar].to_sentence %></p>
      <% end %>

      <%# Inline remove confirmation %>
      <% if Current.user.avatar.attached? && Current.user.avatar.blob.persisted? %>
        <div data-inline-confirm-target="actions" style="margin-top: 0.5rem;">
          <button type="button" class="btn btn--danger btn--sm"
                  data-action="click->inline-confirm#confirmRemove">
            Remove Photo
          </button>
        </div>
        <%= render "shared/inline_confirm", message: "Are you sure you want to remove your profile picture?" %>
        <%= form_with url: avatar_account_path, method: :delete,
              data: { "inline-confirm-target": "deleteForm" } do %>
        <% end %>
      <% end %>
    </div>

    <%# Username row %>
    <div class="settings-info-row" data-controller="settings-row-edit">
      <div class="settings-info-row__left">
        <div data-settings-row-edit-target="display">
          <div class="settings-info-row__label">Username</div>
          <div class="settings-info-row__value"><%= Current.user.display_name %></div>
        </div>

        <div data-settings-row-edit-target="form" hidden>
          <%= form_with url: account_path, method: :patch do |f| %>
            <%= f.hidden_field :section, value: "profile" %>
            <div class="settings-inline-edit">
              <div data-controller="field-error">
                <%= f.text_field :display_name,
                      value: Current.user[:display_name],
                      placeholder: Current.user.display_name,
                      class: "form-control",
                      maxlength: 50,
                      data: { field_error_target: "input", action: "blur->field-error#validate input->field-error#clear" } %>
                <small class="form-hint">Leave blank to use your email username (<%= Current.user.email_address.split("@").first %>)</small>
                <span class="field-error" data-field-error-target="message"
                      <% if Current.user.errors[:display_name].any? %>data-server-error="true"<% else %>hidden<% end %>
                      aria-live="polite"><%= Current.user.errors[:display_name].first %></span>
              </div>
              <div class="settings-inline-edit-actions">
                <%= f.submit "Save", class: "btn btn--primary btn--sm" %>
                <button type="button" class="btn btn--secondary btn--sm"
                        data-action="settings-row-edit#cancel">Cancel</button>
              </div>
            </div>
          <% end %>
        </div>
      </div>

      <div class="settings-info-row__right" data-settings-row-edit-target="editBtn">
        <button type="button" class="settings-edit-link"
                data-action="settings-row-edit#edit">Edit</button>
      </div>
    </div>

    <%# Email row %>
    <div class="settings-info-row" data-controller="settings-row-edit">
      <div class="settings-info-row__left">
        <div data-settings-row-edit-target="display">
          <div class="settings-info-row__label">Email</div>
          <div class="settings-info-row__value"><%= Current.user.email_address %></div>
        </div>

        <div data-settings-row-edit-target="form" hidden>
          <%= form_with url: account_path, method: :patch do |f| %>
            <%= f.hidden_field :section, value: "email" %>
            <div class="settings-inline-edit">
              <div data-controller="field-error">
                <%= f.email_field :email_address,
                      value: Current.user.email_address,
                      required: true,
                      autocomplete: "username",
                      class: "form-control",
                      data: { field_error_target: "input", action: "blur->field-error#validate input->field-error#clear" } %>
                <span class="field-error" data-field-error-target="message"
                      <% if Current.user.errors[:email_address].any? %>data-server-error="true"<% else %>hidden<% end %>
                      aria-live="polite"><%= Current.user.errors[:email_address].first %></span>
              </div>
              <div data-controller="field-error">
                <%= f.password_field :current_password,
                      required: true,
                      placeholder: "Current password",
                      autocomplete: "current-password",
                      class: "form-control",
                      data: { field_error_target: "input", action: "blur->field-error#validate input->field-error#clear" } %>
                <span class="field-error" data-field-error-target="message"
                      <% if Current.user.errors[:current_password].any? %>data-server-error="true"<% else %>hidden<% end %>
                      aria-live="polite"><%= Current.user.errors[:current_password].first %></span>
              </div>
              <div class="settings-inline-edit-actions">
                <%= f.submit "Save", class: "btn btn--primary btn--sm" %>
                <button type="button" class="btn btn--secondary btn--sm"
                        data-action="settings-row-edit#cancel">Cancel</button>
              </div>
            </div>
          <% end %>
        </div>
      </div>

      <div class="settings-info-row__right" data-settings-row-edit-target="editBtn">
        <button type="button" class="settings-edit-link"
                data-action="settings-row-edit#edit">Edit</button>
      </div>
    </div>

    <%# Account type row %>
    <div class="settings-info-row">
      <div class="settings-info-row__label">Account type</div>
      <div class="settings-info-row__right">
        <select class="settings-select-control" disabled>
          <option>Student</option>
        </select>
      </div>
    </div>

  </div>

  <%# Change password link %>
  <a href="#" class="settings-password-link"
     data-controller="settings-row-edit"
     style="display:none">Change password</a>

  <%# Password change — accessible via a subtle card below %>
  <details class="settings-info-card" style="margin-top: 0.5rem; overflow: hidden;">
    <summary style="padding: 1rem 1.5rem; font-size: 0.875rem; font-weight: 600; color: var(--color-muted); cursor: pointer; list-style: none; display: flex; align-items: center; justify-content: space-between;">
      Change password
      <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 20 20" fill="currentColor">
        <path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd"/>
      </svg>
    </summary>
    <div style="padding: 0 1.5rem 1.5rem; border-top: 1px solid var(--color-border-light);">
      <%= form_with url: account_path, method: :patch do |f| %>
        <%= f.hidden_field :section, value: "password" %>
        <div style="margin-top: 1rem; display: flex; flex-direction: column; gap: 0.75rem;">
          <div class="form-group" data-controller="password-toggle field-error">
            <%= f.label :current_password, "Current password", style: "font-size:0.875rem;" %>
            <div class="field-with-toggle">
              <%= f.password_field :current_password, required: true,
                    autocomplete: "current-password", class: "form-control",
                    data: { password_toggle_target: "field", field_error_target: "input",
                            action: "blur->field-error#validate input->field-error#clear" } %>
              <button type="button" class="auth-input__toggle"
                      data-action="password-toggle#toggle"
                      aria-label="Toggle password visibility">
                <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 20 20" fill="currentColor">
                  <path d="M10 12a2 2 0 100-4 2 2 0 000 4z"/>
                  <path fill-rule="evenodd" d="M.458 10C1.732 5.943 5.522 3 10 3s8.268 2.943 9.542 7c-1.274 4.057-5.064 7-9.542 7S1.732 14.057.458 10zM14 10a4 4 0 11-8 0 4 4 0 018 0z" clip-rule="evenodd"/>
                </svg>
              </button>
            </div>
            <span class="field-error" data-field-error-target="message"
                  <% if Current.user.errors[:current_password].any? %>data-server-error="true"<% else %>hidden<% end %>
                  aria-live="polite"><%= Current.user.errors[:current_password].first %></span>
          </div>
          <div class="form-group" data-controller="password-toggle field-error">
            <%= f.label :password, "New password", style: "font-size:0.875rem;" %>
            <div class="field-with-toggle">
              <%= f.password_field :password, required: true,
                    autocomplete: "new-password", minlength: 8, maxlength: 72, class: "form-control",
                    data: { password_toggle_target: "field", field_error_target: "input",
                            action: "blur->field-error#validate input->field-error#clear" } %>
              <button type="button" class="auth-input__toggle"
                      data-action="password-toggle#toggle"
                      aria-label="Toggle password visibility">
                <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 20 20" fill="currentColor">
                  <path d="M10 12a2 2 0 100-4 2 2 0 000 4z"/>
                  <path fill-rule="evenodd" d="M.458 10C1.732 5.943 5.522 3 10 3s8.268 2.943 9.542 7c-1.274 4.057-5.064 7-9.542 7S1.732 14.057.458 10zM14 10a4 4 0 11-8 0 4 4 0 018 0z" clip-rule="evenodd"/>
                </svg>
              </button>
            </div>
            <small class="form-hint">Minimum 8 characters</small>
            <span class="field-error" data-field-error-target="message"
                  <% if Current.user.errors[:password].any? %>data-server-error="true"<% else %>hidden<% end %>
                  aria-live="polite"><%= Current.user.errors[:password].first %></span>
          </div>
          <div class="form-group" data-controller="password-toggle field-error">
            <%= f.label :password_confirmation, "Confirm new password", style: "font-size:0.875rem;" %>
            <div class="field-with-toggle">
              <%= f.password_field :password_confirmation, required: true,
                    autocomplete: "new-password", maxlength: 72, class: "form-control",
                    data: { password_toggle_target: "field", field_error_target: "input",
                            action: "blur->field-error#validate input->field-error#clear" } %>
              <button type="button" class="auth-input__toggle"
                      data-action="password-toggle#toggle"
                      aria-label="Toggle password visibility">
                <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 20 20" fill="currentColor">
                  <path d="M10 12a2 2 0 100-4 2 2 0 000 4z"/>
                  <path fill-rule="evenodd" d="M.458 10C1.732 5.943 5.522 3 10 3s8.268 2.943 9.542 7c-1.274 4.057-5.064 7-9.542 7S1.732 14.057.458 10zM14 10a4 4 0 11-8 0 4 4 0 018 0z" clip-rule="evenodd"/>
                </svg>
              </button>
            </div>
            <span class="field-error" data-field-error-target="message"
                  <% if Current.user.errors[:password_confirmation].any? %>data-server-error="true"<% else %>hidden<% end %>
                  aria-live="polite"><%= Current.user.errors[:password_confirmation].first %></span>
          </div>
          <%= f.submit "Update password", class: "btn btn--primary btn--sm" %>
        </div>
      <% end %>
    </div>
  </details>

  <%# Appearance %>
  <p class="settings-section-label">Appearance</p>
  <div class="settings-info-card">

    <div class="settings-info-row">
      <div class="settings-info-row__label">Theme</div>
      <div class="settings-info-row__right">
        <select class="settings-select-control" disabled>
          <option>Light</option>
          <option>Dark</option>
        </select>
      </div>
    </div>

    <div class="settings-info-row">
      <div class="settings-info-row__label">Language</div>
      <div class="settings-info-row__right">
        <select class="settings-select-control" disabled>
          <option>English (UK)</option>
          <option>English (US)</option>
        </select>
      </div>
    </div>

  </div>

  <%# Danger zone %>
  <div class="settings-danger-zone" data-controller="inline-confirm">
    <div class="settings-danger-zone__row">
      <div class="settings-danger-zone__text">
        <h2 class="settings-danger-zone__title">Delete Account</h2>
        <p class="settings-danger-zone__description">Permanently delete your account and all associated data. This action cannot be undone.</p>
      </div>
      <div>
        <div data-inline-confirm-target="actions">
          <button type="button" class="btn btn--danger"
                  data-action="click->inline-confirm#confirmRemove">
            Delete Account
          </button>
        </div>
        <%= render "shared/inline_confirm", message: "Are you sure you want to delete your account?" %>
      </div>
    </div>
    <%= form_with url: account_path, method: :delete,
          data: { "inline-confirm-target": "deleteForm" } do %>
    <% end %>
  </div>

</div>
