<span id="follower-count-<%= user.id %>" class="profile__stat">
  <strong><%= localized_number(user.followers_count) %></strong>
  <%= t("follows.follower_label", count: user.followers_count) %>
</span>
