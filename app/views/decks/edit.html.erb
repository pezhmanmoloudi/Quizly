<% content_for :title, t("decks.edit.title") %>

<div class="container">
  <div class="nd-split">
    <div class="nd-split__header">
      <h1 class="nd-split__title"><%= t("decks.edit.title") %></h1>
    </div>
    <%= render "form", deck: @deck %>
  </div>
</div>
