<div id="<%= dom_id(deck, :in_folder) %>" class="folder-deck-card">
  <%= render "decks/deck_card", deck: deck,
        due_counts: local_assigns[:due_counts],
        last_studied_dates: local_assigns[:last_studied_dates],
        folder: folder %>
</div>
