<% content_for :title, t("passwords.edit.title") %>

<div class="auth-split">
  <div class="auth-split__form">
    <%= link_to login_path, class: "auth-split__logo", dir: "ltr" do %>
      <svg width="28" height="28" viewBox="0 0 28 28" fill="none" xmlns="http://www.w3.org/2000/svg">
        <rect width="28" height="28" rx="7" fill="currentColor" opacity="0.15"/>
        <text x="14" y="19" text-anchor="middle" font-size="14" font-weight="700" fill="currentColor">Q</text>
      </svg>
      Quizly
    <% end %>

    <h1 class="auth-split__heading"><%= t("passwords.edit.heading") %></h1>
    <p class="auth-split__sub"><%= t("passwords.edit.subheading") %></p>

    <%= form_with url: reset_password_path(params[:token]), method: :patch do |f| %>
      <div class="form-group" data-controller="password-toggle field-error">
        <%= f.label :password, t("passwords.edit.password_label") %>
        <div class="auth-input">
          <svg class="auth-input__icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
            <path fill-rule="evenodd" d="M5 9V7a5 5 0 0110 0v2a2 2 0 012 2v5a2 2 0 01-2 2H5a2 2 0 01-2-2v-5a2 2 0 012-2zm8-2v2H7V7a3 3 0 016 0z" clip-rule="evenodd"/>
          </svg>
          <%= f.password_field :password, required: true,
                autocomplete: "new-password", minlength: 8, maxlength: 72,
                class: "form-control", placeholder: t("passwords.edit.password_placeholder"),
                data: { password_toggle_target: "field", field_error_target: "input",
                        action: "blur->field-error#validate input->field-error#clear" } %>
          <button type="button" class="auth-input__toggle"
                  data-action="password-toggle#toggle"
                  aria-label="<%= t("passwords.edit.toggle_password") %>">
            <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 20 20" fill="currentColor">
              <path d="M10 12a2 2 0 100-4 2 2 0 000 4z"/>
              <path fill-rule="evenodd" d="M.458 10C1.732 5.943 5.522 3 10 3s8.268 2.943 9.542 7c-1.274 4.057-5.064 7-9.542 7S1.732 14.057.458 10zM14 10a4 4 0 11-8 0 4 4 0 018 0z" clip-rule="evenodd"/>
            </svg>
          </button>
        </div>
        <span class="field-error" data-field-error-target="message" hidden aria-live="polite"></span>
      </div>

      <div class="form-group" data-controller="password-toggle field-error">
        <%= f.label :password_confirmation, t("passwords.edit.confirm_label") %>
        <div class="auth-input">
          <svg class="auth-input__icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
            <path fill-rule="evenodd" d="M5 9V7a5 5 0 0110 0v2a2 2 0 012 2v5a2 2 0 01-2 2H5a2 2 0 01-2-2v-5a2 2 0 012-2zm8-2v2H7V7a3 3 0 016 0z" clip-rule="evenodd"/>
          </svg>
          <%= f.password_field :password_confirmation, required: true,
                autocomplete: "new-password", maxlength: 72,
                class: "form-control", placeholder: t("passwords.edit.confirm_placeholder"),
                data: { password_toggle_target: "field", field_error_target: "input",
                        action: "blur->field-error#validate input->field-error#clear" } %>
          <button type="button" class="auth-input__toggle"
                  data-action="password-toggle#toggle"
                  aria-label="<%= t("passwords.edit.toggle_password") %>">
            <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 20 20" fill="currentColor">
              <path d="M10 12a2 2 0 100-4 2 2 0 000 4z"/>
              <path fill-rule="evenodd" d="M.458 10C1.732 5.943 5.522 3 10 3s8.268 2.943 9.542 7c-1.274 4.057-5.064 7-9.542 7S1.732 14.057.458 10zM14 10a4 4 0 11-8 0 4 4 0 018 0z" clip-rule="evenodd"/>
            </svg>
          </button>
        </div>
        <span class="field-error" data-field-error-target="message" hidden aria-live="polite"></span>
      </div>

      <%= f.submit t("passwords.edit.submit"), class: "btn btn--primary btn--block" %>
    <% end %>

    <p class="auth-split__footer">
      <%= link_to t("passwords.edit.back"), login_path %>
    </p>
  </div>

  <%= render "auth_panel" %>
</div>
