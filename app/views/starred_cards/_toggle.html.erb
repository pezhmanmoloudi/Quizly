<div id="star_<%= card_progress.id %>">
  <% if card_progress.starred? %>
    <%= button_to card_progress_starred_card_path(card_progress),
          method: :delete,
          class: "star-btn star-btn--active",
          title: t("starred_cards.unstar") do %>
      ★
    <% end %>
  <% else %>
    <%= button_to card_progress_starred_card_path(card_progress),
          method: :post,
          class: "star-btn",
          title: t("starred_cards.star") do %>
      ☆
    <% end %>
  <% end %>
</div>
