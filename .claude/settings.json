{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // empty' | { read -r f; echo \"$f\" | grep -qE '\\.(rb|rake)$' && bin/rubocop -f github \"$f\" 2>&1 || true; } 2>/dev/null || true",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
